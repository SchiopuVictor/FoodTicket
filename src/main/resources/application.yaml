spring:
  application:
    name: FoodTicket
  datasource:
    username: root
    password: admin123
    url: jdbc:mysql://localhost:3306/FoodTicket?crateDatabaseIfNotExist=true
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        generate_statistics: true
logging:
  level:
    org:
      hibernate:
        stat: DEBUG
